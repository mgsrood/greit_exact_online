from setuptools import setup, find_packages

setup(
    name='utils', #needs to build fabric
    version='1.0.12',
    description='Supporting utilities for the Fabric Notebooks',
    packages=find_packages(),  
    install_requires=[],
)